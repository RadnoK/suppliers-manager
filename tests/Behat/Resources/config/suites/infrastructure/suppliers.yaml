default:
    suites:
        infrastructure_suppliers:
            contexts_services:
                - app.behat.context.application.supplier
            filters:
                tags: "@suppliers && @api"
